import React from "react";
import { InputTextStyled, InfoImgStyled, InfoTitleStyled } from './styles/component-styled-constants';

  const Info = ({title, icon}) => {
    return (<InputTextStyled onChange={onChange} >
  </InputTextStyled>)
  }

  export default Info;